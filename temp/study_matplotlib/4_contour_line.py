'''Python可视化库matplotlib.pyplot里contour与contourf的区别    https://blog.csdn.net/cymy001/article/details/78513712'''import numpy as npimport matplotlib.pyplot as pltfrom matplotlib.colors import ListedColormap'''contour和contourf都是画三维等高线图的，不同点在于contourf会对等高线间的区域进行填充，区别如下：'''# 使用 contourf 画出等高线def contourf():    x = np.array([1, 2])    y = np.array([1, 2])    z = np.array([[1, 2], [2, 3]])    plt.xlim(1, 2)    plt.ylim(1, 2)    colors = ('red', 'blue', 'lightgreen', 'gray', 'cyan')    cmap = ListedColormap(colors[:len(np.unique(z))])    plt.contourf(x, y, z, cmap=cmap)  ###    plt.show()    plt.close()    pass# 使用 contour 画出等高线def contour():    x = np.array([1, 2])    y = np.array([1, 2])    z = np.array([[1, 2], [2, 3]])    plt.xlim(1, 2)    plt.ylim(1, 2)    colors = ('red', 'blue', 'lightgreen', 'gray', 'cyan')    cmap = ListedColormap(colors[:len(np.unique(z))])    plt.contour(x, y, z, cmap=cmap)  ###    plt.show()    plt.close()    passif __name__ == '__main__':    contour()